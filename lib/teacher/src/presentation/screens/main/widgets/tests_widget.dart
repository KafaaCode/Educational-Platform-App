import 'package:flutter/material.dart';

class TestsWidget extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Center(
      child: Text('صفحة الاختبارات'),
    );
  }
}
