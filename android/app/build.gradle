plugins {
    id "com.android.application"
    id "kotlin-android"
    // The Flutter Gradle Plugin must be applied after the Android and Kotlin Gradle plugins.
    id "dev.flutter.flutter-gradle-plugin"
}

android {
    namespace = "com.example.educational_platform_app"
    compileSdk = flutter.compileSdkVersion
    ndkVersion = flutter.ndkVersion

    compileOptions {
        sourceCompatibility = JavaVersion.VERSION_1_8
        targetCompatibility = JavaVersion.VERSION_1_8
    }

    kotlinOptions {
        jvmTarget = JavaVersion.VERSION_1_8
    }

    // flavorDimensions "app"
    // productFlavors {
    //     student {
    //         dimension "app"
    //         applicationId "com.example.educationalplatform.student"
    //         versionCode 1
    //         applicationIdSuffix ".student"
    //         versionName "1.0"
    //         resValue "string", "app_name", "Student App"
    //     }
    //     teacher {
    //         dimension "app"
    //         applicationId "com.example.educationalplatform.teacher"
    //         resValue "string", "app_name", "Teacher App"
    //         applicationIdSuffix ".teacher"
    //         versionCode 1
    //         versionName "1.0"
    //     }
    // }

    defaultConfig {
        // Remove the specific applicationId from here since it's defined in flavors
        minSdk = flutter.minSdkVersion
        targetSdk = flutter.targetSdkVersion
        versionCode = flutter.versionCode
        versionName = flutter.versionName
        // missingDimensionStrategy 'app', 'student' // Add this line
    }

    buildTypes {
        release {
            // TODO: Add your own signing config for the release build.
            // Signing with the debug keys for now, so `flutter run --release` works.
            signingConfig = signingConfigs.debug
        }
    }
}

flutter {
    source = "../.."
}

